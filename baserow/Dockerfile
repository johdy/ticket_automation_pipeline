FROM baserow/baserow:1.35.3

# Copie ton CSV d’exemple dans un dossier accessible (optionnel)
COPY ticket_table.csv /baserow/data/ticket_table.csv

# Rien d’autre à faire — baserow démarre automatiquement
# Si tu veux le précharger, tu peux automatiser via l’API (n8n peut s’en charger ensuite)
